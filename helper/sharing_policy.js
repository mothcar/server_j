'use strict'

// const nodemailer = require('nodemailer')
const policy = {
  reward_point : {
    name : 10,
    category : 10,
    floor: 10,
    desc : 20, 
    area : 20, 
    tel  : 10, 
    homepage : 10, 
  }
}



module.exports = policy
